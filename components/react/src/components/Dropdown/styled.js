import styled from 'styled-components';
import { Dropdown as D, Popover as P } from '../../fundamentals-react';

export const DropDown = styled(D)`
  && {
  }
`;

export const Popover = styled(P)`
  && {
    .fd-popover__body {
      border: solid 1px rgba(137, 145, 154, 0.45);
    }
  }
`;
